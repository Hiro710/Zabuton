大喜利イベント情報アプリ

### 使用方法

```
$ git clone リポジトリURL

$ cd zabuton

$ bin/rails db:migrate

# もしマイグレーションが失敗してエラーが出たら以下を実行して再度$ bin/rails db:migrate を実行
$ yarn install --check-files

$ bin/rails s (=> localhost:3000)
```
